## 2024-05-17 - Missing htmlFor attributes in SetupWizard and OnboardingModal
**Learning:** Found that custom forms often miss linking `<label>` with `<input>` or `<textarea>` using `htmlFor` and `id` which negatively affects screen readers and keyboard navigation.
**Action:** Next time, ensure all labels explicitly reference their corresponding input elements for better accessibility.
